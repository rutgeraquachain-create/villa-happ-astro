---
const items = ['Sinds 1945', 'Van Babyparadijs tot Europa', 'The Comeback Kid', 'Genummerde oplage', 'Ontstaan in Tilburg, nu uit Waalwijk', 'Stap voor stap'];
const loop = [...items, ...items];
---
<div class="vh-marquee" aria-hidden="true">
  <div class="vh-marquee-track">
    {loop.map((t) => <span class="vh-marquee-item">{t}</span>)}
  </div>
</div>
